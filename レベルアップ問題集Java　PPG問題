2023年7月問題
<炎上案件>
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        int due = scanner.nextInt();
        scanner.close();

        if (due <= 3) {
            System.out.println("BOMB");
        } else {
            System.out.println("ONGOING");
        }
    }
}
/*if (false) の部分を修正し、due <= 3 を条件に設定しました。これにより、納品まで残り3日以下の場合に条件を満たします。
条件に一致した場合は「BOMB」、それ以外の場合は「ONGOING」を出力するようにしました。
余計な文字や空行がないように、フォーマットも整えました。*/

<不気味な数字>
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        int n = Integer.parseInt(scanner.nextLine());
        scanner.close();

        int ans = n + n + n;
        System.out.println(ans);
    }
}
/*入力を文字列として受け取る代わりに、Integer.parseInt を用いて整数として処理しました。
数値として3回足し算するコードに変更しました。*/
